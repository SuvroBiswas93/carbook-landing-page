# Environment variables
.env
.env*.local
.env.example

# Server runtime data (contains secrets/hashes) - never commit
data/admins.json
data/sessions.json

# Server build artifacts
server/dist
.next
.kilo
node_modules/

# TypeScript build info
*.tsbuildinfo

# Stale npm lockfile (project uses pnpm)
package-lock.json